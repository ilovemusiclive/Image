<h3>Show event page</h3>

<h2><%= @event.name %></h2>

<%= link_to "Back", events_path %>
<%= link_to "edit", edit_event_path(@event) %>
<%= link_to "Delete", event_path(@event), method: :delete, data: { confirm: "Are you sure?" } %>
